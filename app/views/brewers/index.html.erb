<h1>Brewers</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @brewers.each do |brewer| %>
  <tr>
    <td><%= link_to brewer.name, brewer %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New brewer', new_brewer_path %>